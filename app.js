// Hostinger LiteSpeed / Passenger Node entry point (v2.0.4 - Fix dashboard stats, Google Drive images, translations, mojikake cleanup, fast counts)
const app = require('./src/server.js');

module.exports = app;
