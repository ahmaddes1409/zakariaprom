// Hostinger LiteSpeed / Passenger Node entry point (v2.2.0 - Add category dropdown filter and quick category chips to admin products search)
const app = require('./src/server.js');

module.exports = app;
