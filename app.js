// Hostinger LiteSpeed / Passenger Node entry point (v2.0.9 - Fix Ofset Baskı category display on homepage, auto-clean Drive URL, add admin delete category)
const app = require('./src/server.js');

module.exports = app;
